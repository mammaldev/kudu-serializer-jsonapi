{
  "name": "kudu-serializer-jsonapi",
  "version": "0.5.2",
  "main": "lib/serializer.js",
  "scripts": {
    "compile": "babel -d lib/ src/",
    "lint": "eslint {src,test}/*.js; true",
    "test": "mocha test/ -R spec --recursive --compilers js:babel-register",
    "prepublish": "npm run compile"
  },
  "peerDependencies": {
    "kudu": "^1.0.0-alpha.1"
  },
  "devDependencies": {
    "babel-cli": "^6.4.0",
    "babel-eslint": "^4.1.6",
    "babel-preset-es2015": "^6.3.13",
    "babel-register": "^6.4.3",
    "chai": "^3.4.1",
    "eslint": "^1.9.0",
    "kudu": "^1.0.0-alpha.1",
    "mocha": "^2.3.3"
  }
}
